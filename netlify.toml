[build]
  command = "curl -sSL https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_Linux-64bit.tar.gz | tar -xz -C /tmp && /tmp/hugo --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.140.2"
